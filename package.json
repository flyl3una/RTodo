{
  "name": "rtodo",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
